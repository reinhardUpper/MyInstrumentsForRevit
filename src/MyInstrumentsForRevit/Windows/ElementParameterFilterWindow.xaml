<Window x:Class="MyInstrumentsForRevit.Windows.ElementParameterFilterWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Фильтр по параметру элемента"
        Width="620"
        Height="460"
        MinWidth="480"
        MinHeight="340"
        WindowStartupLocation="CenterScreen"
        ResizeMode="CanResize"
        KeyDown="OnWindowKeyDown">
    <Grid Margin="12">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>

        <TextBox x:Name="SearchTextBox"
                 Height="30"
                 Margin="0,0,0,8"
                 VerticalContentAlignment="Center"
                 TextChanged="OnSearchTextChanged" />

        <ListBox x:Name="ParameterListBox"
                 Grid.Row="1"
                 MouseDoubleClick="OnIsolateClick"
                 SelectionChanged="OnSelectionChanged">
            <ListBox.ItemTemplate>
                <DataTemplate>
                    <Grid Margin="2,4">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="*" />
                            <ColumnDefinition Width="Auto" />
                        </Grid.ColumnDefinitions>
                        <StackPanel>
                            <TextBlock Text="{Binding Name}"
                                       FontWeight="SemiBold"
                                       TextTrimming="CharacterEllipsis" />
                            <TextBlock Text="{Binding DisplayValue}"
                                       Foreground="#666666"
                                       FontSize="12"
                                       TextTrimming="CharacterEllipsis" />
                        </StackPanel>
                        <Border Grid.Column="1"
                                Margin="8,0,0,0"
                                Padding="6,2"
                                CornerRadius="3"
                                Background="#E7E7E7"
                                VerticalAlignment="Center">
                            <TextBlock Text="{Binding StorageTypeText}"
                                       FontSize="11"
                                       Foreground="#555555" />
                        </Border>
                    </Grid>
                </DataTemplate>
            </ListBox.ItemTemplate>
        </ListBox>

        <TextBlock x:Name="StatusTextBlock"
                   Grid.Row="2"
                   Margin="0,8,0,8"
                   Foreground="#666666"
                   Text="Выберите параметр." />

        <StackPanel Grid.Row="3"
                    Orientation="Horizontal"
                    HorizontalAlignment="Right">
            <Button Content="Изолировать"
                    Width="110"
                    Height="30"
                    Margin="0,0,8,0"
                    Click="OnIsolateClick" />
            <Button Content="Скрыть"
                    Width="90"
                    Height="30"
                    Margin="0,0,8,0"
                    Click="OnHideClick" />
            <Button Content="Отмена"
                    Width="90"
                    Height="30"
                    IsCancel="True" />
        </StackPanel>
    </Grid>
</Window>
